<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.example.project.greenAuth.repository.Mapper">

	<select id="findNum">
		SELECT auth_num, name, company, part, day, end_day
		FROM GREEN_AUTHENTICATION ga WHERE AUTH_NUM = #{authNum}
	</select>
</mapper>